# =====================================================================
# Crossplane Claim — XPostgreSQLInstance
# =====================================================================

apiVersion: database.poc.io/v1alpha1
kind: PostgreSQLInstance
metadata:
  name: app-demo-db
  namespace: default
spec:
  parameters:
    dbName: app_demo
    owner: platform
    connectionLimit: 20